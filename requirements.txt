# requirements.txt

# Core Frameworks
fastapi==0.110.0
uvicorn[standard]==0.29.0 # For running the FastAPI app
httpx==0.27.0 # For making async HTTP requests (to Ollama, web search APIs, etc.)
pydantic==2.6.4 # For data validation and settings

# LLM & RAG
google-generativeai==0.5.2

# Vector Store (FAISS)
faiss-cpu==1.8.0 # Or faiss-gpu if you have the hardware and setup for it
numpy==1.26.4 # Often a dependency for FAISS and numerical operations
packaging==23.2 # ADDED: For FAISS runtime import

# Document Loading & Web Search
python-dotenv==1.0.1 # For loading .env files
googlesearch-python==1.2.3 # For the web_search.py (if you keep that logic)
beautifulsoup4==4.12.2 # For parsing HTML (used by web_search.py)
requests==2.31.0 # Often pulled in by other libraries or for simple sync calls

# Note: pyside6 is a GUI dependency and has been removed for the backend image.
# Note: 'ollama' is a service, not a pip-installable Python library for this context.